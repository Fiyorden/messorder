<div class="btn-group">
  <%= link_to icon('fa-solid','pencil'), edit_wallet_path(@wallet), class: "btn btn-info" %>
  <%= link_to icon('fa-solid','list'), wallets_path, class: "btn btn-secondary" %>
  <%= button_to icon('fa-solid','times'), @wallet, method: :delete, class: "btn btn-danger" %>
</div>

<%= render @wallet %>
<%= simple_form_for wallet do |f| %>
  <%= f.input :in, collection: [["Entrant", true],["Sortant", false]], as: :radio_buttons %>
  <%= f.association :user %>
  <%= f.input :amount %>

  <%= f.button :submit %>
<% end %>

<%= simple_form_for dish do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.association :type %>
  <%= f.input :picture, as: :file%>
  <%= f.input :enabled, as: :radio_buttons %>
  <%= f.button :submit %>
<% end %>

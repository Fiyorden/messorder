<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
    <%= link_to "Mess Order", root_url, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a href="#" class="nav-link" aria-current="page">Mon compte</a></li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Administrateur
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to Dish.model_name.human.pluralize.mb_chars.titleize, dishes_path, class: "dropdown-item #{active_class(dishes_path)}" %></li>
            <li><%= link_to Opening.model_name.human.pluralize.mb_chars.titleize, openings_path, class: "dropdown-item #{active_class(openings_path)}" %></li>
            <li><%= link_to Order.model_name.human.pluralize.mb_chars.titleize, orders_path, class: "dropdown-item #{active_class(orders_path)}" %></li>
            <li><%= link_to Type.model_name.human.pluralize.mb_chars.titleize, types_path, class: "dropdown-item #{active_class(types_path)}" %></li>
            <li><%= link_to Wallet.model_name.human.pluralize.mb_chars.titleize, wallets_path, class: "dropdown-item #{active_class(wallets_path)}" %></li>
          </ul>
        </li> 
      </ul>
      <% if not current_user %>
        <%= link_to icon('fa-solid', 'right-to-bracket') + " se connecter ", new_user_session_path, class: "btn btn-outline-success me-2" %>
      <% else %>
        <%= link_to icon('fa-solid', 'arrow-right-from-bracket') + " se déconnecter ", nil, class: "btn btn-outline-danger me-2" %>
      <% end %>
    </div>
  </div>
</nav>